Test sito — copia e test

Questa cartella è vuota al momento. Posso copiarci dentro una versione del sorgente C (`Sartoria.c`) con una modalità `--selftest` se desideri che CI o test locali la eseguano in maniera automatica.

Dimmi se vuoi che copi il sorgente principale qui (creo una copia e la adatto per test automatici).